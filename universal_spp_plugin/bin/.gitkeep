# The plugin runs the native converter from this folder: uspp_tool.exe on Windows,
# uspp_tool on Linux. It is not committed. Download the matching release payload,
# run build.ps1 on Windows, or run ./build.sh on Linux.
# Override the location with the USPP_TOOL env var (see ../lib/runner.py).
